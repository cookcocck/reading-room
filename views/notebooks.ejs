<!-- ═══════════════════════════════════════════
   NOTEBOOKS — Premium Redesign
   ═══════════════════════════════════════════ -->

<div class="nb-container">

  <!-- ── Hero ── -->
  <header class="nb-hero">
    <div class="nb-hero-ornament" aria-hidden="true">笔记</div>
    <h1 class="nb-hero-title">札记</h1>
    <p class="nb-hero-lede">
      <span><%= notebooks.length %> 册有笔记</span>
      <span class="nb-hero-sep">·</span>
      <span><%= summary.totalNoteCount.toLocaleString() %> 条笔记</span>
    </p>
    <div class="nb-hero-rule"></div>
  </header>

  <!-- ── Books with Notes Grid ── -->
  <section class="nb-books-section">
    <h2 class="nb-section-title">读书手札</h2>
    <% if (notebooks.length > 0) { %>
    <p class="nt-note">一纸一墨，皆是读过的痕迹。</p>
    <div class="nt-wall" id="nt-wall">
      <% notebooks.forEach(function(nb, i) { %>
      <a class="nt-slip" href="/book/<%= nb.book_id %>" title="<%= nb.title %>" style="animation-delay: <%= Math.min(i * 30, 600) %>ms">
        <span class="nt-slip-head"><%= nb.title %></span>
        <span class="nt-slip-hairline"></span>
        <span class="nt-slip-text"><% if (nb.note_text) { %><%= nb.note_text.length > 150 ? nb.note_text.substring(0, 150) + '…' : nb.note_text %><% } else { %>此书有笔记 <%= nb.total_notes %> 则，暂未落成文字。<% } %></span>
        <span class="nt-slip-foot">
          <span class="nt-slip-seal"><%= nb.total_notes %> 则</span>
          <span class="nt-slip-author"><%= nb.author || '佚名' %></span>
        </span>
      </a>
      <% }); %>
    </div>
    <script>
    (function() {
      var wall = document.getElementById('nt-wall');
      if (!wall) return;
      var slips = Array.prototype.slice.call(wall.children);
      function colCount() { return window.innerWidth <= 768 ? 2 : 4; }
      function build() {
        var n = colCount();
        if (wall.dataset.cols == n) return;
        wall.dataset.cols = n;
        wall.innerHTML = '';
        for (var c = 0; c < n; c++) {
          var col = document.createElement('div');
          col.className = 'nt-col';
          wall.appendChild(col);
        }
        var cols = wall.children;
        function shortest() {
          var m = 0;
          for (var i = 1; i < cols.length; i++) {
            if (cols[i].offsetHeight < cols[m].offsetHeight) m = i;
          }
          return m;
        }
        slips.forEach(function(s) { cols[shortest()].appendChild(s); });
      }
      build();
      window.addEventListener('resize', build);
    })();
    </script>
    <% } else { %>
    <div class="nb-empty">
      <div class="nb-empty-icon">—</div>
      <p>还没有笔记</p>
      <p class="nb-empty-sub">读过的书，留下的思考，都会出现在这里</p>
    </div>
    <% } %>
  </section>

  <!-- ── Recent Notes Feed ── -->
  <div class="nb-rule"></div>
  <section class="nb-feed-section">
    <h2 class="nb-section-title">最近笔记</h2>
    <% if (recentNotes.length > 0) { %>
    <div class="nb-feed">
      <% recentNotes.forEach(function(note) { %>
      <div class="nb-feed-item nb-feed-<%= note.type %>">
        <div class="nb-feed-icon">
          <% if (note.type === 'highlight') { %>
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 20h9"/><path d="M16.5 3.5a2.121 2.121 0 0 1 3 3L7 19l-4 1 1-4L16.5 3.5z"/></svg>
          <% } else { %>
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/></svg>
          <% } %>
        </div>
        <div class="nb-feed-body">
          <div class="nb-feed-text"><%= note.text.length > 140 ? note.text.substring(0, 140) + '…' : note.text %></div>
          <div class="nb-feed-meta">
            <a href="/book/<%= note.book_id %>" class="nb-feed-book"><%= note.book_title %></a>
            <% if (note.chapter) { %>
            <span class="nb-feed-sep">—</span>
            <span class="nb-feed-chapter"><%= note.chapter %></span>
            <% } %>
            <span class="nb-feed-date"><%= helpers.formatTimestamp(note.create_time) %></span>
          </div>
        </div>
      </div>
      <% }); %>
    </div>
    <% } else { %>
    <div class="nb-empty">
      <p>还没有笔记</p>
    </div>
    <% } %>
  </section>

</div>
